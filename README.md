# Practice SLURM - A batch job scheduler for high performance computing

## Cluster provisioning with Vagrant

Install the plugin:

```bash
vagrant plugin install vagrant-libvirt
```

Provision the nodes:
```bash
vagrant up
```
